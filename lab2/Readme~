реализован простой эхо-сервер, изначально возвращающий клиенту его же данные, а затем предлагает вводить любые данные, 
при этом каждый раз возвращает их ему обратно.

Запускать исполняющийся файл нужно по примеру : ./server INET TCP 6889 1
где server - исполняющийся файл
    INET   - Internet-домен
    TCP    - протокол
    6889   - порт ( можно любой свободный )
    1      - количество подключаемых клиентов

